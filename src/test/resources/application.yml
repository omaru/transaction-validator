spring:
  application:
    name: transaction-validator
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:latest:///transaction_validator
    username: user
    password: password
  flyway:
    enabled: true
  jpa:
    properties:
      hibernate:
        default_schema: "transaction_validator"
